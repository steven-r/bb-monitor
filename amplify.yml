version: 1
applications:
  - appRoot: apps/bb-monitor
    frontend:
      phases:
        prebuild:
          commands:
            - npm install -g nx
            - npm install
        build:
          commands: 
            - npm install -g nx
            - npm run build
      artifacts:
        baseDirectory: ../../dist/apps/bb-monitor
        files:
          - '**/*'
      cache:
        paths:
          - ../../node_modules/**/*
