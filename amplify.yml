version: 1
applications:
  - appRoot: ./
    frontend:
      phases:
        prebuild:
          commands:
            - yarn install
        build:
          commands: 
            - yarn build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
