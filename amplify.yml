version: 1
applications:
  - appRoot: apps/bb-monitor
    frontend:
      phases:
        prebuild:
          commands:
            - npm ci
        build:
          commands: 
            - npm run build
      artifacts:
        baseDirectory: ../../dist/apps/bb-monitor
        files:
          - '**/*'
      cache:
        paths:
          - ../../node_modules/**/*
