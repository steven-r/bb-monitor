version: 1
applications:
  - appRoot: apps/bb-monitor
    frontend:
      phases:
        prebuild:
          commands:
            - yarn install
        build:
          commands: 
            - yarn nx build bb-monitor
      artifacts:
        baseDirectory: ../../dist/apps/bb-monitor
        files:
          - '**/*'
      cache:
        paths:
          - ../../node_modules/**/*
