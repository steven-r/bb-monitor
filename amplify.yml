version: 1
applications:
  - appRoot: src/
    frontend:
      phases:
        prebuild:
          commands:
            - yarn install
        build:
          commands: 
            - npm build
      artifacts:
        baseDirectory: build
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
