version: 1
applications:
  - appRoot: apps/bb-monitor
    frontend:
      phases:
        prebuild:
          commands:
            - cd ../..
            - npm install -g nx
            - npm install
        build:
          commands: 
            - npm install -g nx
            - nx build bb-monitor --prod
      artifacts:
        baseDirectory: ../../dist/apps/bb-monitor
        files:
          - '**/*'
      cache:
        paths:
          - ../../node_modules/**/*
